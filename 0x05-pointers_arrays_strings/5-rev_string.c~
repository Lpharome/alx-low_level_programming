/**
 * Project : Thinks in reverse
 * Author : lpharome
 *
 */
#include "main.h"
/**
 * rev_string - Reverses a string.
 * @s: string to be reserved.
 */
void rev_string(char *s)
{
	char s,j;
	int i,k;
		while ((*s+i))

}
